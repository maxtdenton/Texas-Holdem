package combinationChecker
/*
import (
	"fmt"
	"strconv"
)

func NewHand(c1, c2, c3, c4, c5 string) [5]int {
	card1, err := strconv.Atoi(c1)
	card2, err := strconv.Atoi(c2)
	card3, err := strconv.Atoi(c3)
	card4, err := strconv.Atoi(c4)
	card5, err := strconv.Atoi(c5)
	var numbers[5]int
	numbers[0] = card1
	numbers[1] = card2
	numbers[2] = card3
	numbers[3] = card4
	numbers[4] = card5
	if err != nil {
		panic("invalid card suit... what did you do?")
	}
	return numbers
}

func printValues(flushOpinion string, hsChance int, lsChance int, flushStraightComp string) {

}

func FlushStraightComp(hsChance, lsChance, flushNum int) string {

}

func StartChecking(c1, c2, c3, c4, c5 string, flushVal int) {
	var flushOpinion, flushStraightComp string
	var flushNum, hsChance, lsChance int
	var numbers[5]int
	numbers = NewHand(c1, c2, c3, c4, c5)
 	flushOpinion, flushNum = CheckFlush(flushVal)
	hsChance, lsChance = CheckStraight(numbers)
	flushStraightComp = FlushStraightComp(hsChance, lsChance, flushNum)
	printValues(flushOpinion, hsChance, lsChance, flushStraightComp)
}
*/